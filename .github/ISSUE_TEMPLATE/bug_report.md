---
name: Bug report
about: Report a problem so we can fix it
title: "[bug] "
labels: bug
---

**What happened**
A clear description of the bug.

**Expected behavior**
What you expected instead.

**Reproduction**
Steps or a minimal snippet (curl / SDK / CLI) that triggers it.

**Environment**
- stateful.ai version (`GET /health` → version):
- Python version:
- Stores: `RELATIONAL_STORE` / `VECTOR_STORE` / `GRAPH_STORE`:
- Embedding backend:
- Deployment (local / docker / cloud):

**Logs**
Relevant logs (include the `request_id` from the error envelope if present).
